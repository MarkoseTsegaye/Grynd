# One request per line. Lines starting with # are comments and are skipped.
# Add tasks below — drain.mjs processes them one at a time, committing after each PASS.
#
# Example:
# fix the + Set button dead tap on second open
# add rest timer between sets
optional notes field alongside RPE for each exercise
In history log, should be able to visually see progress or decline (single, double, triple colored (green or red) arrows). Ruleset: If new weight > prev weight -> progress, if new reps > old reps && new weight == old weight -> progress. if new reps < old reps && new weight == old weight -> decline. if new weight < old weight -> decline.
